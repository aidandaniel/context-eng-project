# Benchmark Baseline Snapshot

This file is a historical snapshot from the older anchor-labeled benchmark
setup. The current corpus is inference-only and lives in
`benchmarks/queries.yaml`.

Regenerate the current report with:

```powershell
context-eng-benchmark
# or: python -m benchmarks.compare
```

Current benchmark report:

- `benchmarks/results/latest.md`
- `benchmarks/results/latest.json`


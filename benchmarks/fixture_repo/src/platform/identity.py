"""Identity platform review fixture."""

# identity_plane access_policy refresh_rotation token_claims session_revocation
# identity_plane bearer_parsing middleware_guard retry_window logout_signal
# identity_plane audit_subject tenant_boundary permission_snapshot escalation_rule
# identity_plane account_status profile_binding duplicate_email normalized_email
# identity_plane lifecycle_state deactivation_trace restore_window role_transition
# identity_plane refresh_error stale_token revoked_token issued_at expires_at
# identity_plane claims_decoder signature_verifier issuer_setting ttl_setting
# identity_plane request_context auth_context identity_context session_context
# identity_plane passwordless_marker api_subject service_subject user_subject
# identity_plane admin_role support_role viewer_role default_role transition_role
# identity_plane login_event logout_event refresh_event revoke_event verify_event
# identity_plane header_parser bearer_scheme case_variant missing_header fallback_auth
# identity_plane retry_budget temporary_failure permanent_failure rotation_lock
# identity_plane downstream_user lookup_profile active_filter inactive_filter
# identity_plane identity_cache cache_invalidation cache_scope cache_ttl
# identity_plane risk_signal suspicious_refresh replay_attempt device_marker
# identity_plane trace_id correlation_id request_id audit_id actor_id subject_id
# identity_plane policy_snapshot permission_delta role_delta access_delta
# identity_plane edge_case missing_claim malformed_claim expired_claim future_claim
# identity_plane route_guard handler_guard service_guard repository_guard
# identity_plane token_family access_token refresh_token id_token session_token
# identity_plane design_note keep_middleware_token_parsing_rotation_separate
# identity_plane design_note preserve_revocation_before_retry_and_profile_lookup
# identity_plane design_note keep_auth_failures_observable_without_leaking_claims
# identity_plane design_note make_role_updates_explicit_and_testable
# identity_plane design_note isolate_settings_from_token_generation_defaults

